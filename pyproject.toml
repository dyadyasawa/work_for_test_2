[tool.poetry]
name = "work-for-test-2"
version = "0.1.0"
description = "Тестовое задание"
authors = ["Александр"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
